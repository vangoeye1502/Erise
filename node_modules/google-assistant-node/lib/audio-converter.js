"use strict";
var __extends = (this && this.__extends) || (function () {
    var extendStatics = Object.setPrototypeOf ||
        ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
        function (d, b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
var stream = require("stream");
var messages = require('./googleapis/google/assistant/embedded/v1alpha1/embedded_assistant_pb');
var AudioConverter = (function (_super) {
    __extends(AudioConverter, _super);
    function AudioConverter() {
        return _super.call(this, { objectMode: true }) || this;
    }
    AudioConverter.prototype._transform = function (chunk, enc, cb) {
        var buff = Buffer.from(chunk);
        var offset = 0;
        var size = 1024 * 16;
        for (var i = 0; i < chunk.length / size; i++) {
            var nibble = buff.slice(offset, (offset + size));
            offset += size;
            var request = new messages.ConverseRequest();
            request.setAudioIn(nibble);
            this.push(request);
        }
        return cb(null);
    };
    return AudioConverter;
}(stream.Transform));
exports.default = AudioConverter;
//# sourceMappingURL=audio-converter.js.map